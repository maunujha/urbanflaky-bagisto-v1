/**
 * Urbanflaky — Custom JS
 * Global site-wide custom behaviour lives here.
 * No build step required; loaded on every shop page via the layout.
 *
 * Page-specific scripts (e.g. product detail sticky bar) live in
 * their respective Blade views inside @pushOnce('scripts').
 */
